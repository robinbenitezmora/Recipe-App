<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <div class="logo">Logo</div>
      <ul id="links" style='margin-right:20%;'>
        <a class="link-item"><%= link_to "My Recipes", root_path %></a>
        <a class="link-item" style='margin-left:5px;'><%= link_to "My Foods", foods_path %></a>
        <a class="link-item" style='margin-left:5px;'><%= link_to "Recipe foods index", recipe_foods_path %></a>
        <% if user_signed_in? %>
          <a style='margin-left:5px;'><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></a>
          <a style='margin-left:5px;'><%= link_to "Edit Profile", edit_user_registration_path %></a>
        <% else %>
          <a style='margin-left:5px;'><%= link_to "Sign In", new_user_session_path %></a>
          <a ><%= link_to "Sign Up", new_user_registration_path %></a>
        <% end %>
      </ul>
    </header>
    <h3 class="notice"><%= notice %></h3>
    <h3 class="alert"><%= alert %></h3>
    <%= yield %>
  </body>
</html>
